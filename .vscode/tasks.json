{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Test Bat File",
            "type": "shell",
            "command": "${workspaceFolder}\\test.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
            }
        }
    ]
}